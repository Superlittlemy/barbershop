<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.slm.barbershop.mapper.ShopMapper">

    <resultMap id="ShopOverviewStatsVO" type="com.slm.barbershop.model.ShopOverviewStatsVO">
        <result property="shopCount" column="shop_count" />
        <result property="memberCount" column="member_count" />
        <result property="totalBalance" column="total_balance" />
    </resultMap>

    <select id="selectOverviewBase" resultMap="ShopOverviewStatsVO">
        SELECT COUNT(DISTINCT s.id)        AS shop_count,
               COUNT(m.id)                 AS member_count,
               COALESCE(SUM(m.balance), 0) AS total_balance
        FROM shop s
        LEFT JOIN member m ON m.shop_id = s.id AND m.is_deleted = 0
        WHERE s.user_id = #{userId}
          AND s.is_deleted = 0
    </select>

    <select id="sumRecentConsumeAmount" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(b.total_amount), 0)
        FROM bill b
        INNER JOIN shop s ON s.id = b.shop_id AND s.is_deleted = 0
        WHERE s.user_id = #{userId}
          AND b.type = 'CONSUME'
          AND b.is_cancelled = 0
          AND b.is_deleted = 0
          AND b.created_time >= #{since}
    </select>

</mapper>
